// checks if person is the specified group + has edit controls 
// TODO implement group wala middleware
const inGroup = (req, res, next) => {
    // if (!req.group) {

    // }
    next();
}
export default inGroup;